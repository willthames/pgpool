# Options for pgpool

# -n: don't run in daemon mode. does not detatch control tty
# -d: debug mode. lots of debug information will be printed

OPTS=" -n{% if pgpool_debug_level > 0%} -d{% endif %}"
PGPOOLLOG={{pgpool_log_file}}
